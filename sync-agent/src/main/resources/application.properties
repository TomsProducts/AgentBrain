server.url=http://localhost:8080
sync.token=dev-token-change-me
claude.home=${user.home}/.claude
watch.interval.ms=500
local.api.port=7701

# Disable Spring web server - we use our own embedded HttpServer
spring.main.web-application-type=none
